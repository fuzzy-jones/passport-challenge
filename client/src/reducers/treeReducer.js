// import { GET_BRANCHES } from '../actions/types';

const initialState = {
    branches: [],
    branch: {}
};

export default function(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}